# Changelog

Provenance ledger for the skills in `cook/` and `serve/` and the repo machinery around them: what changed, why, and the model/harness state it was earned against. Artifacts carry only a one-line pin (`<!-- Earned against: <model>, <date>, <version> -->`); the narrative, sunset triggers, and re-test verdicts live here. Newest first. Reference skills by name and section — not line numbers, which rot.

## 2026-07-14 — worklog retired; `changelog` folded in from machine scope as the lightweight decision log

- **worklog + worklog-minor deleted; replaced by a single `changelog` skill.** The worklog system (`.worklog/BRIEF.md` derived from an append-only `LOG.md`, four entry types, a significance gate, and inline graduation to `AGENTS.md`/`.claude/rules`) was more machinery than the everyday gap needs. Owner asked for something lightweight, low-token, scannable by human and agent, that does not overlap with git history, auto memory, or transcripts. The heavy pieces (BRIEF projection, graduation) were the cost; the value was the dated why/rejected/open record. `changelog` keeps the value and drops the machinery.
- **The `/changelog` this repo already referenced was a phantom in-repo but real at machine scope.** worklog's SKILL.md deferred "what changed" to "the `/changelog` skill" — no such skill existed in `serve/`. It existed as a standalone real dir at `~/.claude/skills/changelog/` (authored 2026-05-27, single SKILL.md), which is the skill a stackradar session correctly reported as "installed." Personal scope overrides plugin scope, so shipping `serve/skills/changelog` while that copy lives would let the older machine-scope copy silently shadow the kitchen one everywhere. Folded the machine-scope skill into `serve/` as canonical; owner removes the machine-scope copy (below) so the plugin version wins.
- **Scope was set by investigation, not by the request.** Checked the mid-2026 state before building: a live movement (Contextual Commits open standard 2026-03-14, "Lore", Git Context Controller) argues the why/rejected/learned belong in enriched git commit bodies, not a separate file; ADRs remain the committed-markdown standard for why + rejected but are critiqued as going stale against the live system; AGENTS.md cannot absorb history (instruction-following degrades past ~120 lines of project context). Contextual Commits' own month-1 field data undercuts the pure-git position: 72.5% of commits adopted the `[context]` scaffold but only 11.8% filled `rejected:` — a 2.6× structure-vs-substance gap ("hollow compliance"), self-described as "a signal, not proof." Conclusion: a committed file earns its place, but only for the three things git (even enriched) structurally cannot give — rejected approaches that left no commit, the still-open dimension that has no commit to attach to, and a curated newest-first skim surface. The skill pulls Files from `git diff` and writes only Why/Rejected/Open from the session, so it complements git rather than duplicating it.
- **Improvements over the standalone on fold-in.** (1) Lightweight Rejected-pushback: before logging, skim the sparse `**Rejected:**` lines and surface the dated entry if the new work repeats a rejected approach — preserves worklog's most valuable behavior (stop re-walking a dead end) at near-zero read cost, and directly answers the "agents repeat rejected approaches" problem the research names. (2) Broadened collision guard: the standalone only stopped on release-changelog markers; it now also stops on any other structured log (provenance/model-pin ledger, auto-generated file) — which protects this very repo's `CHANGELOG.md` (a provenance ledger) from being appended to if `/changelog` ever runs here. (3) Explicit complements-git-not-a-second-git framing up top. (4) Kitchen provenance pin + README; worklog comparisons removed from body and description.
- **Process skill — probe-exempt.** Like the worklog it replaces, its value is the owner wanting the procedure, which no model release absorbs. CLAUDE.md's process-skill exemption list updated (`worklog, worklog-minor` → `changelog`); `serve` plugin.json and marketplace.json descriptions updated.
- **Machine scope reported, not touched (forge-family boundary).** Owner to run: `rm -rf ~/.claude/skills/changelog` (standalone, now superseded) and `rm ~/.claude/skills/worklog ~/.claude/skills/worklog-minor` (symlinks into `serve/`, now dangling). The kitchen `changelog` reaches other repos through the plugin (push → `/plugin marketplace update claudia` → `/plugin update`), not a machine-scope symlink; `bin/sync-cross-tool` handles `~/.cursor`, `~/.codex`, and repo-local `.claude/skills` and prunes the worklog orphans there.
- Model state: Opus 4.8 (`claude-opus-4-8`), Claude Code v2.1.207. Loader-trigger grep clean; preship-check passes.

## 2026-07-13 — Base UI `data-*` selectors verified against installed contracts; harness-audit: uncommitted guardrails and read-but-skipped prose

- **Two confirmed dead-selector bugs in a production repo (stackradar, Fable 5 session, 2026-07-12/13) earned a mechanical verification step.** `components/ui/toggle.tsx` styled `data-[state=on]:` (twice, incl. the `pill` variant) on Base UI's Toggle, which sets only `data-pressed`/`data-disabled` — confirmed by reading `node_modules/@base-ui/react/toggle/ToggleDataAttributes.d.ts`; the selector was dead from its migration commit onward (git blame: a real migration commit, not vendor boilerplate), masked by a redundant `aria-pressed:` sibling doing the visual work. `components/ui/tooltip.tsx` was worse: three `data-[state=delayed-open]:` selectors in the same className string as the correct `data-open:`/`data-closed:` ones (`TooltipPopupDataAttributes.d.ts` declares no `data-state` at all), live-mounted via sidebar navigation. Both times a Radix→Base migration added the correct selectors and never removed the old idiom beside them — compiles, lints, looks right in the browser. Neither shadcn-tailwind nor design-engineer told the reader to verify a `data-*` selector against the primitive's installed `*DataAttributes.d.ts`; both leaned on live docs or the general bare-attribute vocabulary — true in general, unverified per-component — which is exactly the gap that shipped these.
- **shadcn-tailwind: "Style state through the data attributes" gains the verification paragraph.** Read the part's `*DataAttributes.d.ts` under `node_modules/@base-ui/react/<component>/` before judging any `data-*` selector correct *or* wrong; vocabularies are per-component (Toggle has no open/closed) and clearing one clears nothing else; dead selectors survive behind redundant siblings; the toggle/tooltip incident cited in-body as neutral fact. Replaces the softer "check the API reference rather than guessing" line. Done-checklist state-attribute item extended to cover selectors *already in the file*, where a working sibling can mask a dead one. Probe 8 added (toggle fixture with the masking `aria-pressed:` sibling and the real `.d.ts` in `node_modules`; pass requires reading and citing the installed file — the right verdict from memory fails, because the earning incident is asserting from recall until confronted). Probe 8's Fable 5 baseline recorded from the production session with an explicit not-clean-room caveat. Pin untouched.
- **shadcn-tailwind: vendor migration skill named as the migration authority.** shadcn made Base UI the default and ships an official migration skill (`pnpm dlx skills add shadcn/ui`; per-component, flags behavior changes instead of silently patching, leaves a report per component in `.migration/`) — verified against ui.shadcn.com/docs/changelog, 2026-07-13. The stack-assumptions section now routes actual migrations there, keeps this skill as the always-on layer for code written after, and points hand-migrated components at the `.d.ts` check first. The skill's existing detection doctrine (`components.json` + deps, never version numbers) held up in the incident repo unchanged.
- **design-engineer: reference pointers only, no body change.** checklist.md's state-variant item and motion-base-ui.md's popup-animation example each gain a one-sentence verify-the-`.d.ts` pointer (the tooltip bug was animation selectors — squarely this skill's pre-ship territory); the full procedure lives in shadcn-tailwind only (same plugin, auto-loads on the same files; duplicating it violates the blast-radius line both skills carry). Pin untouched.
- **harness-audit: enforcement parity gains step zero (committed-guardrail check) and a prose-failure tier.** Same session, second incident: stackradar's CLAUDE.md "Source hierarchy" section plus a `globals.css` permission-gate bullet — written by a prior Opus session specifically to prevent a repeat of an earlier incident — had never been committed (`git log -S"Source hierarchy"` empty; an 18-line uncommitted working-tree diff). Check 3 now opens with `git status`/`git diff`/`git log -S` over the instruction chain: an uncommitted guardrail is one checkout away from not existing and has propagated nowhere. Third incident, same session: the standing instruction "read installed sources, cite them" was present and loaded, and going to source still took direct confrontation, twice — check 3 now tiers prose-only orders, putting read-but-skipped ones at the head of the hook/gate candidates instead of the stay-manual list (prose is re-weighed against everything else in context every session). Pin untouched.
- **`~/.agents/skills/verify-base-ui-attrs` (ad-hoc session stopgap) superseded by the merge — reported, machine scope, not touched.** Decision: merge into shadcn-tailwind rather than keep standalone. The check's failure shape (looks right in the browser) never prompts anyone to invoke a separate skill; shadcn-tailwind auto-loads on the exact paths (`components/ui/*.tsx`) where the check matters, so it fires without being remembered, and a second artifact teaching data-attribute correctness would drift against the first. Owner can retire the standalone copy once serve propagates (push → `/plugin marketplace update claudia` → `/plugin update`).
- Model state: Fable 5 (`claude-fable-5`), Claude Code v2.1.207. Loader-trigger grep clean on all touched files.

## 2026-07-12 — forge: CLAUDE.md orientation is a job, "point don't mirror"

- **The CLAUDE.md guidance was framed purely defensively and under-served orientation.** `references/always-on.md` led with what to *cut* ("Never current state") and listed *pointers* only as "where the rest of the discipline lives, named once." Read straight, that framing deletes a CLAUDE.md's most useful positive job for a cold agent — orienting it to the shape of the system, where the load-bearing pieces live, and the non-obvious surfaces (a `bin/` script that composes several steps, a prod-only config flag, a runtime limit that only bites on large inputs). The gap surfaced against a good real-world exemplar (a single-file QB64 web-server CLAUDE.md): its Architecture and Common-Commands sections are high-value orientation, yet the old framing would flag them for removal as "the code already says that."
- **Fix: orientation named as a first-class job, with `point, don't mirror` as the operative test.** Added a block to always-on.md distinguishing the durable *pointer* ("routing is in `handle_request`", "constants at the top of `app.bas`") from the rotting *mirror* ("`MAX_CLIENTS = 8`", "16 pages", reproduced tree/dep list). The asymmetry is the argument: a pointer survives the *common* edit (changing a value, adding a page) and rots only on a rare rename; a mirror is wrong on the common edit and misleads at recurring cost for zero discovery saved. The dividing test is discovery cost, not "is it about the code." One contrast example only (forge's own 3×-cost rule). Also corrected the "goes elsewhere" table row that flatly cut all build/test commands — a `bin/` script whose composed behavior its name doesn't reveal is a pointer worth keeping; `npm test` still goes.
- **Probe added as a guidance-validation fixture, not a forge death-test.** `evals/claude-md-orientation-probe.md`: two probes against a scratch single-file-server fixture — bootstrap ("write a CLAUDE.md") and the load-bearing mirror-temptation ("document the architecture … include the key configuration", where "configuration" baits value-copying). Gates whether the orientation block earns its place: if Opus 4.8 both orients and refuses the mirror unaided, the block is restating a default and should be cut. Baseline not yet run (real tokens, opt-in). Forge stays process-exempt from a death-test probe; this fixture validates one expertise claim, it is not a deletion gate for the skill.
- **Pin untouched** (revisions log here). Loader-trigger grep clean on both touched files. SKILL.md not edited — the always-loaded surface defers CLAUDE.md mechanics to this reference, kept lean.

## 2026-07-11 — probe runners: unset ANTHROPIC_OAUTH_TOKEN in the nested-CLI spawn

- **First external run of the repo surfaced an env-leak in both probe runners.** `run-probe.sh` and `gen-probe.sh` spawned the nested `claude` CLI with only `env -u CLAUDECODE`. When the runner is itself invoked from inside a Claude Code session — the natural way a visitor's agent runs it — the parent session's `ANTHROPIC_OAUTH_TOKEN` is inherited, and that token is session-scoped: the nested CLI 401s instead of falling back to stored login. Fix: `env -u CLAUDECODE -u ANTHROPIC_OAUTH_TOKEN`, a no-op when the var is absent (plain-terminal runs unaffected). Externally reproduced fix (a community tester's agent hit the 401 and produced the identical patch); the scripts' existing "do not inherit nested-session env" guard was the right intent, incomplete by one variable.
- Same-day observation from that test: the repo builds and runs first-go on an outside machine, but the README gives a visitor no first-ten-minutes path — philosophy is clear, entry point is not. Logged as an open README gap, not yet actioned.

## 2026-07-11 — shadcn-tailwind re-aimed: stack sunset fired via minor drift; forge learns procedures-over-facts

- **The stack-keyed sunset (logged 2026-06-17) fired without a version major.** Full review against live docs, the shadcn MCP, and transcript harvests of four project archives (nui, CLN, templates-default, this repo). Two sections had inverted from stale to actively wrong: Base UI's package renamed `@base-ui-components/react` → `@base-ui/react` (the skill's own detection grep matched nothing in any owner repo, silently concluding the stack was absent), and the "`data-state` is the styling API" section taught the Radix idiom — Base UI exposes bare attributes (`data-open`, `data-closed`, `data-pressed`, `data-starting-style`/`data-ending-style`; verified against base-ui.com and all three owner repos, where generated components run 27:5 bare-vs-bracket with the stragglers being TanStack Table's own `data-state="selected"`). Also corrected: Base UI became the shadcn *default* 2026-07 with Radix fully supported on 4.x (detection now reads `components.json` `style: base-*` + deps, never version numbers), and the token system is two-layer (`@import "shadcn/tailwind.css"` base package + `globals.css` delta; `shadcn eject` inlines).
- **Re-aimed from conventions tutor to owner discipline.** The 2026-07-05 probe verdict (4/5 absorbed) plus the vendor now shipping its own project-aware agent skill and MCP left the conventions half absorbed twice over. Harvest evidence set the new lead: the untouchable-holy-grail inversion (six sessions — parallel `DataTable`, second breadcrumb, per-instance card styling, primitives re-implemented losing stock behavior) now opens the skill as named failure shapes with the missing-stock-behavior smell test. Added from recurrence: the design-handoff contract (values are never new; named utility → token → stop and ask; derive from referenced in-app surfaces), the moved bracket gap (on-scale values inside `size-*`/radius/grid templates; fractional steps; `--spacing()` composition), ratified px carve-outs (shadow offsets/blur, iOS 16px input floor, hit-area floors — shadow colors stay oklch), the protected token layer (propose `globals.css` edits, never side-effect; never switch the dark-mode mechanism), verify-before-done (token→class proof, tailwind-merge `classGroups` registration for custom `--text-*`, paired line-heights, computed-styles over assertion, drive the interaction), the links-as-buttons `nativeButton={false}` recipe, and registry-before-hand-building (shadcn MCP tools, GitHub registries, base-ui.com/llms.txt over memory). Cut: client/server boundary section (other surfaces own it); shrunk: uncontrolled-by-default and `render` (absorbed; kept as trap notes). Re-pinned Fable 5, 2026-07-11, v2.1.204.
- **Probes:** fixture dep corrected to `@base-ui/react`; probe 6 added (bare data attributes vs `data-[state=]`) and probe 7 (edit the shared card source, not call sites); probes 1–5 and all baseline verdicts untouched; probe 4's n≥2 re-run still pending before any further shrink.
- **design-engineer references — currency fixes only** (motion-base-ui.md package import + popup example to bare attributes; checklist.md state-variant line). Pin untouched.
- **forge — two doctrine additions from this event.** "Encode the discovery, not the facts" extended to vendor facts (package names, attribute idioms, version-keyed claims rot worst — they invert to actively wrong; a bare vendor fact in a body is a re-test liability the probe file should cover), and ship-section sunset line widened: stack-keyed triggers include vendor default flips, package renames, idiom changes, not just majors. New Skills bullet: a correction recurring in sessions where the skill was loaded means wording/salience, not triggering — rewrite the rule as its concrete failure shapes and lead with the highest-recurrence one (the harvest showed the ui/-inversion and bracket corrections recurring with this skill demonstrably in context). Pin untouched.
- **Reported, machine scope, not touched:** vercel plugin's shadcn skill carries a validate rule on `components.json` (`"base": "base-ui"` → warns to reinit `--base radix`) plus Radix-first framing — it fires on the exact files this skill owns and can steer an agent to flip a Base UI project; owner options are per-repo `enabledPlugins` or accepting the standing conflict. `~/.agents/skills/tailwind-design-system` teaches Radix `Slot`/`asChild` as the canonical Button; `~/.claude/skills/figma-to-tailwind-tokens` forbids all bracket values (now aligned with this skill's stop-and-ask stance, but its memorized additive radius math is stale vs the multiplicative scale owner repos use); `emil-design-eng` still imports from `framer-motion` and leads with Radix CSS vars.

## 2026-07-10 — quality-audit: server-side security and state-integrity dimensions

- **The skill's security lens was entirely client-delivery; the launch-sinking failures are server-side.** Source: a circulating "audit your vibe-coded app before launch" prompt the owner supplied for triage. Verdict on its five sections: accessibility and visual-consistency **rejected** — the skill's existing G and E checklists are stricter and more concrete (keypair, `file:line`, specific SC numbers vs. the prompt's open prose); security, concurrency, and reliability **named real gaps**. Dimension L covered deps, HTML sinks, headers, SRI, cookies, and source maps — nothing on authz re-checks in route handlers and server actions, client-trusted tenant IDs (IDOR / cross-tenant), BaaS row-level security, injection sinks, secret exposure via `NEXT_PUBLIC_`, SSRF, uploads, or webhook signatures. Nothing anywhere covered duplicate submissions, non-idempotent mutations, effect/subscription cleanup, out-of-order async responses, or missing loading/error/empty states.
- **Encoded as two new gated dimensions rather than a fatter L:** `dimensions.md` section **M. Server-side security & data exposure** (applies only when server code or a DB/auth/BaaS SDK is detected — a static frontend skips it; carries the trace-flows-end-to-end instruction, since the vulnerability is usually the check a flow skips, not a line a grep finds) and **N. State integrity & failure handling** (any app with mutations or async data; no specialist skill routes here, so the fallback checklist is the depth). SKILL.md routing table, Step 0 detection signals, and description updated; L's header now points server-side concerns at M. The stock `security-review` skill is referenced as diff-scoped depth only.
- **The harvest is the checklist content, not the prompt's shape.** The source prompt is one undifferentiated "investigate everything" wall — exactly the ad-hoc single pass the skill exists to replace (no stack gating, no severity triage, no honest-verification discipline). Its accessibility section also asks for less than the skill already requires, evidence that generic circulating prompts trail the encoded checklists on covered ground while still exposing uncovered ground.
- **Pin untouched** (revision, logs here). Loader-trigger grep clean. No probe: process skill, exempt.

## 2026-07-08 — harness-audit: value counterweight (cost is not the verdict)

- **The skill measured only cost and always concluded "cut."** Reported behavior: in a repo where the owner relied on plugins she valued, `harness-audit` read the whole harness as overhead and drove toward disabling them — "cost is one thing, but this?" The gap: every check and the unit itself (`tokens-per-session-forever`) count the denominator; nothing weighed what a surface lets the owner produce, so the skill's logical endpoint was the empty harness. This is the kitchen's own meta-trap (CLAUDE.md: "scoring an artifact on what's easy to measure while the output goes ungraded") pointed at the audit itself. Fixed in three places: the stance names the third failure (treating cost as the verdict) beside the two it already named; Step 4 gains a use-gate above the buckets (removing or disabling a capability is never **Applied** at any scope, and a disable becomes a recommendation only where the owner confirms she does not use the surface — a used surface is reported at its cost, never cut); the "Equating more skills with more capability" anti-pattern, which tilted toward treating every skill as suspect, is replaced by "Recommending a cut you never use-tested," keeping the true half (the *unchosen* marginal artifact taxes) without the tilt.
- **Also closed the residual auto-apply ambiguity.** The **Applied** bucket read "project-local settings in *this* repo's `.claude/`," which a project-scope plugin toggle in `settings.local.json` satisfies — so a disable could be auto-applied even though **Proposed** listed plugin toggles as ask-first. Applied is now "reversible *and capability-preserving* … never a disable or a deletion"; capability removal routes through the use-gate regardless of scope.
- **The machine-scope and silent-disable behavior the owner also hit was a stale install, not a live gap.** The `claudia` marketplace cache served to other repos was pinned at `1bd86e0`, five commits behind `origin/main` — predating the same-day `a80a00d` report-only boundary and Applied/Proposed/Reported triage. Those complaints resolve on `/plugin marketplace update claudia` → `/plugin update`; this entry's value-lens fix needs the same propagation once merged.
- **Pin untouched** (revisions log here). Loader-trigger grep clean. No probe: process/audit skill, exempt.

## 2026-07-08 — forge family: machine scope is report-only; harness-audit reads CLAUDE.md as intent

- **Hard scope boundary added after a production miss.** A `harness-audit` run offered "which of these should I prepare exact changes for? (I'll show each before applying)" aimed at machine-scope config (personal skills, global plugins, user-scope hooks). Owner correction: *none of forge should touch machine-wide settings, ever.* The family's write scope is the **current repo**; everything under `~/.claude/`, user/enterprise settings, and global plugins is inventory-and-report only — never written, staged, or offered for apply, even when the fix is one obvious line. Encoded in three places so it holds in the kitchen and travels with the plugin: `harness-audit` Step 4 (renamed "Triage"; buckets are now **Applied** / **Proposed** / **Reported** / **Held**, with the machine-scope boundary stated above them and a new anti-pattern naming the exact offending question), `forge` SKILL ("Scope: build in-repo, never reach into machine scope" line after the pin paragraph), and kitchen CLAUDE.md "Other general rules." The `Proposed` bucket keeps the legitimate part of the old `Recommended` — this-repo behavior-config the auto-mode classifier gates, applied on an explicit yes.
- **Supersedes the earlier "cross-repo config as ready-to-apply choices" pattern** (2026-06-10 auto-memory `harness-audit-pattern`, recorded from a session where she approved cross-repo options). That was situational approval; the standing rule is now report-only at machine scope. Memory rewritten; MEMORY.md index hook updated.
- **`harness-audit` now audits the CLAUDE.md chain as intent, not just for cost** (owner request, with a liked exemplar). New section "The CLAUDE.md chain, read as intent" checks: a **code-authority clause** (code wins over the doc; a contradiction means the doc is stale — flagged as the single highest-leverage line, her favorite in the exemplar), **restated current state** (stack/deps/scripts/layout the code already owns — grep versions and dep names against the manifests), **frozen facts that should be discovered** (read-first instructions over pasted snapshots), **bare directives** (condition-shaped + reasoned vs "Y before Z"), **unverified mechanisms**, and the **~14-rule / example-3× ceilings**. Draws on forge's own `CLAUDE.md and rules` principles (Never-current-state, encode-the-discovery, reasoned rules) recast as audit checks; a soft one-directional pointer hands authoring back to forge. The content-review anti-pattern was narrowed from "any content review" to "*skill-body* content review" so it no longer forbids the CLAUDE.md read — CLAUDE.md is the setup spine, within this skill's remit; individual skill bodies stay the forge's.
- **Pins untouched** (revisions log here, not in the pin). Loader-trigger grep clean on both skills. No probe: `harness-audit` is a process/audit skill (exempt, like worklog/quality-audit).

## 2026-07-08 — STATE/HACKS bump to v2.1.204

- **STATE.md + HACKS.md rolled v2.1.201 → v2.1.204** (changelog delta v2.1.202–204, near-entirely bug fixes). Three entries cleared the "changes how you build/configure/ship" filter: the **Dynamic workflow size** `/config` setting (v2.1.202), the restored **`/review <pr>` (fast single-pass) vs `/code-review <level> <pr#>` (multi-agent)** split (v2.1.202), and **MCP `roots/list`** now carrying the session's `--add-dir` roots plus a `notifications/roots/list_changed` (v2.1.203). Everything else filtered out (workflow OTel attrs, login-expiry warning, ⏸ manual-mode badge, "agents re-delegate less", re-invoke-skill dedup fix, left-arrow view-close).
- **Out-of-band correction: Fable 5 subscription-inclusion window extended 2026-07-07 → 2026-07-12** (official @claudeai post, 2026-07-07; ≤50% weekly cap unchanged, then pay-as-you-go $10/$50). Neither the window nor its extension ever hit the CC changelog — caught by the Step 0 model-availability check, fixed in STATE ×2 and HACKS ×1.
- **CLAUDE.md sweep clean; cook/serve sweep clean.** No rule contradicted (no new model shipped, so no pin re-test triggered). No artifact references the stale July-7 date or the touched surfaces; the forge defers workflow-API specifics to the live Workflow tool description, and quality-audit's `/code-review` references are diff-level (unaffected by the PR-review split).

## 2026-07-08 — intake reframe: earn on the gap, not the failure

- **The earning frame was failure-centric across every surface; corrected.** The forge stance, harvest, probe doctrine, CLAUDE.md provenance, and README all framed an artifact's origin as a *failure* (or an authoritative source). In practice the everyday means are a raw idea worth operationalizing and a miscommunication (the output missed intent, refined so it doesn't recur); failure is one means, not the main one. Reframed the earning bar to **a gap between the model's default and the owner's intent** — idea/source, miscommunication, post-cutoff fact, or failure. Failure kept as a valid path (speed-daemon et al. genuinely earned off one); the taste skills' owner-columns already encoded intent-not-failure.
- **Touched:** CLAUDE.md (kitchen-optimizes-for spine + pinning doctrine), forge SKILL (description stance, earned-bar #2, probe/replay lines), forge `references/skills.md` (probe-file definition), harvest (attention line + `earned-intent` handoff label), README (loop, HARVEST, SERVE claim, three-bars).
- **Convergent evidence:** the failure-earned skills are the shrink/deletion candidates; the idea/intent-earned ones (saltintesta, zoom-out, STATE/HACKS) are the keepers — the frame now fits the keepers.

## 2026-07-08 — probe batch guardrails (session-limit incident)

- **Parallel headless probe batches can drain a Max session limit in one wave.** A publish-readiness run launched 23 nested `claude -p` Fable sessions at 5–6-way parallelism, hit the session limit, then relaunched 15 “critical” runs on reset (including ones already complete) and burned the fresh limit again in ~55s. Not an infinite loop — nested CLI turns share one account session quota, high `--max-turns` (16–28 vs probe-file convention of 12), and no skip-existing / confirm gate.
- **Harness added:** `bin/grade-probe` (deterministic, zero tokens), `bin/gen-probe` (single-turn `--tools ""`), `bin/run-probes` (full agent replay, last resort). Unsafe scratchpad `run-batch*.sh` disabled. Forge `references/skills.md` carries the cost doctrine.
- **Enforcement, not just guidance:** the safe runners cap concurrency internally, but the incident came from an agent hand-writing `xargs -P 6 ... claude -p` and bypassing them. `.claude/hooks/no-parallel-claude.sh` (PreToolUse Bash, wired in committed `settings.json` beside `preship-gate`) blocks agent-typed parallel/headless-`claude` fan-out — `xargs -P ≥2`, GNU `parallel`, loops/backgrounding with `claude`, or ≥2 `claude` commands in one call — and points to the safe path. Distinguishes the `claude` command from `.claude/` paths and `claude-*` path segments (17/17 block-vs-allow tests). Human override: `PROBE_ALLOW_PARALLEL=1`. Hooks for guarantees, skills for guidance.
- **Publish-readiness report** (`cook/skills/forge/evals/publish-readiness-2026-07-08.md`): 4/6 probed expertise skills ready (2 shrink candidates), saltintesta blocked on owner column (`saltintesta-owner-grade-2026-07-08.md`), zoom-out pending fixture legs. README SERVE claim narrowed to expertise/taste skills + process exempt class.

## 2026-07-07 — Fable 5 (`claude-fable-5`), Claude Code v2.1.201

- **zoom-out revised — the clean-context dispatch leaks the project's self-description.** Production observation on first post-authoring use: a cold-read subagent dispatched from inside this repo inherited CLAUDE.md and auto-memory while judging a piece derived from kitchen doctrine — the exact self-description the read exists to strip. The run stayed usable only because the reader disclosed its contamination unprompted; nothing in the skill required that. Convergent with the saltintesta `evals/probes.md` caveat that `--safe-mode` leaks account-level context: clean rooms are a spectrum, and the failure is undisclosed contamination, not contamination itself. "Strip your own context" gains the mechanism line (neutral-directory dispatch, or mandatory disclosure weighed into the verdict). Pin updated.
- **zoom-out candidates, n=1 each, not yet encoded:** (1) hand the clean reader an explicit judging bar (CITE / KEEP / CUT against "would a stranger expert keep this?") instead of an open "review this"; (2) ask the clean reader for the idea the work points at but never lands — on first use it surfaced a different unfinished idea than the session's own read, and the piece needed both. A second occurrence earns each a body line.
- **zoom-out validation, no change:** "Promote the buried best idea" performed as written on first use — the two-column adjudication doctrine buried in saltintesta's `evals/probes.md` (owner verdict rules; mechanical/owner divergence harvests new tells) was promoted to a central role in a written piece and became its strongest section.
- **Convention tightened: pins record the earning event only.** Second pass on the 2026-06-10 one-line-pin convention, which re-drifted because `re-tested KEPT …` / `revised …` clauses are made of models and dates and so pass as pin material — five pins (forge, ingest, speed-daemon, quality-audit, zoom-out) had accreted event chains, triple-storing what `evals/probes.md` baseline tables and this file already hold. All five shrunk to birth form (`Earned against: <model>, <date>, <version> — history: CHANGELOG.md`); every removed clause verified present in this file before deletion. The ambiguity is now closed where it is read (CLAUDE.md provenance section, forge pin doctrine) and enforced (`bin/preship-check` warns on a second event in a pin, advisory, same as the length warning). Context on the pin's standing after probe files: its remaining jobs are the staleness signal at point-of-read and the sole re-test trigger for artifacts that can't carry probes (hooks, rules, CLAUDE.md entries, exempt process skills); release-sweep selection by pin is ceremonial at current fleet size — the 2026-07-05 baseline run swept everything regardless.
- **saltintesta pinned — the fleet's oldest unpinned artifact.** `preship-check` inventory showed it as the only unpinned serve skill; git history dates creation to 2026-05-10 (`a518fe7`, "new skill: saltintesta"), pre-dating the changelog (oldest section 2026-05-14) and the pin discipline itself. Pin added: Opus 4.7, 2026-05-10; CC version omitted as unrecorded rather than approximated. The vintage is the point for a taste skill — the tells were calibrated against Opus 4.7 output, two model lines back, which is exactly the staleness signal a pin exists to carry at point-of-read.

## 2026-07-06 — Fable 5 (`claude-fable-5`), Claude Code v2.1.201

- **New serve skill: `zoom-out`** — strategic review of a whole project against its actual purpose: evidence over self-description, re-derive the real opponents, follow the effort not the story, hygiene ≠ strategy, promote the buried best idea, decide rather than defer, self-audit in writing, one-sentence verdict. Earned from production observation (2026-07-04/05, Opus 4.8 and Fable 5): asked to review a body of work, the unaided model produces a compliance pass inside the given frame — grades hygiene, stays symmetric, returns option menus — and the stance that produced the 2026-07-05 kitchen reorientation had to be supplied by hand as a long prompt. That reorientation is the worked example. Probes defined (frame-taking, deference, effort-vs-story) with dual tells/owner columns; they need a constructed fixture (drifted history + buried idea + doc-code contradiction), not yet built or run. Sunset is owner-governed: the generic moves may absorb, but decide-over-defer and question-the-question are owner preference — tells alone don't delete.
- **zoom-out revised same day after a cross-check against anthropics/skills** (repo HEAD 2026-07-01, fetched 2026-07-06). Finding: no Anthropic skill covers strategic review, bias-stripping, or forward assessment — the check ran against their techniques instead. Stolen: doc-coauthoring's reader-testing insight (a fresh instance with no context bleed catches what authors can't see) became the "Strip your own context" section — clean-context fork reads, blind comparison, the stranger-project verdict; skill-creator's undertriggering warning added fresh-eyes / unbiased / what-could-this-become triggers to the description. Gap closed against owner intent: the inventor move ("Imagine what could be" — forward extrapolation grounded in observed assets) was in the earning instance but missing from the first body; probe 4 added for it. Rejected deliberately: doc-coauthoring's staged interactive questioning (option-menu deference is this skill's named anti-pattern) and skill-creator's viewer/benchmark machinery (the kitchen's probe files already hold that job).
- **forge references/skills.md — probe paragraph notes constructed fixtures**: judgment/stance skills fail only against a project whose story and evidence disagree, so their probes must specify a reconstructible fixture inside probes.md (zoom-out is the worked example). Prompt-only probes silently under-test these skills.
- README reworked around the reorientation: thesis block carries the two-opponents/four-sources framing, COOK gains the loop line and harvest entry, SERVE carries the standing claim and probe-replay mechanics, CHANGELOG.md named as the provenance ledger.

## 2026-07-05 — Fable 5 (`claude-fable-5`), Claude Code v2.1.201

- **Working model is Fable 5 again** (selected 2026-07-05, post-restoration). Supersedes the 07-04 "pins not re-tested / user decision" bullet with a decision: Sonnet 5's release triggers nothing — every pin is Opus 4.8, which is unchanged and still the Max/API default. The live trigger is Fable 5 adoption. Batch re-tests reconstructed from changelog prose rejected as the mechanism; earning failures move into **per-skill runnable probe files** instead, and Fable 5 re-tests run through that conversion, prioritized by which skills actually get used. design-engineer is first in line after speed-daemon — the largest generic-craft holding, so the most exposed to absorption by a stronger model.
- **speed-daemon — re-test protocol extracted into `evals/probes.md`** (first worked example of the probe-file pattern). Five probes — toggle / inline edit / delete / auth gate / expensive input — each with a verbatim prompt, unaided-failure signature, and pass criterion; the 2026-05-29 Opus 4.8 verdicts recorded as the baseline (toggle absorbed, the rest earning). Body keeps the single-probe-trap lesson and points at the file; the ~10 lines of maintainer protocol no longer load into every working invocation. Deletion rule: all probes must pass unaided. Not yet run on Fable 5 — real tokens, opt-in.
- **forge — bar #1 gains the delta-sources line**: the expertise delta has few durable sources (owner's taste operationalized, local truths the model can't know, verified post-cutoff currency, observed failures); an artifact drawing on none competes with the model's own training data and gets absorbed within a release or two. Codifies the kitchen's mandate — produced skills must beat out-of-the-box scaffolds *specifically*, not generically — as a triage question forge asks before craft. Pin revised.
- **STATE.md — lean-system-prompt line made verification-honest.** The feature is changelog-only, never documented; where Sonnet 5/Fable 5 land is unknown (searched 2026-07-05). Rewritten to the verified core (shipped default with Opus 4.8, v2.1.154) plus a check-it-yourself pointer (`/context`), dropping the model-family enumeration that had silently gone stale.
- **The kitchen reoriented around its actual opponents** (owner directive, 2026-07-05): the unaided frontier model — which improves every release and absorbs generic craft — and the owner's time; not other skill scaffolds. Durable value has four sources the model can't have (taste operationalized, local truths, post-cutoff currency, observed failure history) plus one process moat: the standing claim that everything in serve provably beats the unaided current model at its job. CLAUDE.md gains the mission block; forge bar #2 points at harvest for failure evidence; forge craft gains "encode the discovery, not the facts" (promoted from always-on.md's personal-rules guidance to a general principle); the output-verdict bullet gains the taste-vs-expertise calibration split — taste skills grade against their owner, named tells are the mechanical floor, the owner's ear the ceiling.
- **Probe files are now shipping doctrine.** forge "Ship, then let it die well" requires `evals/probes.md` on expertise and taste skills; the model-release re-test is a replay, not a prose reconstruction; references/skills.md carries the mechanics (structure, `--safe-mode` unaided baselines — path-triggered auto-load contaminates them otherwise — verdict recording, the taste dual-column rule). Probe files written: design-engineer (5 — probes 1/5, frequency and anti-slop, carry the taste claim and decide its life), shadcn-tailwind (5 — render-not-asChild the strongest currency claim), saltintesta (3 — dual verdict columns; mechanical-clean-but-owner-rejected outputs harvest new tells), flavored-md (4 — exact alert syntax sharpest). quality-audit, worklog, worklog-minor classified **exempt**: process skills, whose value is the owner wanting the procedure — a release can't absorb that. All new baselines "not yet run"; no unaided baseline was recorded at authoring, so first runs establish them (opt-in, real tokens). Nothing deleted today — the machinery for confident deletion is what was built; first probe runs are expected to shrink or kill something, design-engineer's mechanical probes the likeliest first casualties.
- **harvest (new cook skill)** — the intake stage of the loop: mines session transcripts (`~/.claude/projects/<slug>/*.jsonl`, 0.5–3.5 MB each) for corrections, repeats, and failure smells. Bundled `scripts/extract-user-turns.sh` (jq recipe validated against real transcripts this session) reduces a transcript to timestamped user turns, stripping command noise, tool results, and subagent sidechains; one reader agent per transcript on a quote-only contract; Phase 2 dedups against the standing harness — an already-encoded correction that *recurs* is a compliance finding (artifact not steering), not a new-artifact candidate; evidence bar ≥2 sessions or one costly failure; survivors hand to forge with their quotes as the earned-failure spec. Worked example in hand: the first live extraction surfaced the 2026-06-14 saltintesta correction ("focusing on the wording, not the subject") and traced it to its encoding in the skill body and auto-memory — found on disk, deduped correctly. Scope contract: current project by default, cross-project only when named; quotes corrections, never tool output. `disable-model-invocation: true`. Manifests updated (cook plugin.json + marketplace.json), `bin/sync-cross-tool` run (+1).
- **state-bump — two-jobs framing.** Steps 0–5 named the publication bump (STATE.md/HACKS.md serve their own readers), Steps 6–7 the kitchen sweep (serves cook); the model-release path now inventories probe files for replay alongside pins.
- **First probe baselines run (Fable 5, all 22 probes, `claude --safe-mode --model fable -p`, scratch fixtures, n=1).** Verdicts, recorded per file: **speed-daemon KEPT, 4/5 earning** — all three mutations came back pessimistic (`useMutation` + invalidate-on-success), auth render gated on `isLoading`; only the expensive-input probe absorbed (`useDeferredValue`). Headline: the toggle case Opus 4.8 had absorbed (2026-05-29) **regressed to failing on Fable 5** — absorption is not monotonic across model lines; doctrine line added to forge references/skills.md (probes never retire while the skill lives; shrink wants n≥2). **design-engineer KEPT, shrink candidate** — mechanics absorbed (token-layer clamp, `min-h-dvh`), the frequency taste probe failed (model animated a 100+/day surface with high craft and zero pushback), anti-slop passed. **shadcn-tailwind KEPT, major shrink candidate** — 4/5 absorbed incl. `render`-not-`asChild` (prediction inverted: the currency claim fell first, the px/hex discipline claim held — model wrote `text-[13px] text-[#6b7280]` under designer-spec pressure). **flavored-md KEPT** — exactly its predicted split: alert syntax and `<details>` fold earn; table form and anti-decoration absorbed. **saltintesta pending** — mechanical tells pass (2 borderline); unaided outputs preserved at `evals/baselines/2026-07-05-fable5/` for the owner columns, which rule. Run caveats: n=1 throughout, two design-engineer runs truncated at max-turns, one fixture contained a `render=` exemplar (probe 2's pass partly in-context), and `--safe-mode` leaks account-level context (an unaided output knew the owner's product domain) — baselines are near-clean, not clean-room. Shrink edits deferred pending n≥2 re-runs of the decisive probes.
- **harvest — first production run** (kitchen last 12 sessions; CLN-CLN last 12, scoped by the owner after the auto-mode classifier denied the broader sweep — the denial enforced harvest's own scope contract, which counts as the contract working). Yield: five candidates presented (no-shortcuts spirit line; promised-work-not-landed closeout discipline; plain-English register; CLN dependency hygiene; CLN console-cleanliness constraint) plus one demand signal (portable component-block pattern requested from CLN). Two clusters correctly deduped as already-encoded (the 2026-06-14 saltintesta articulation correction; the 2026-06-17 when_to_use/eval-authority corrections — both predate the artifacts that encode them). Extractor refined from run evidence: task-notification, skill-boilerplate marker, and session-rename noise classes filtered; reader contract gains the boilerplate-skip line.

## 2026-07-04 — Opus 4.8 (`claude-opus-4-8`), Claude Code v2.1.201

- **STATE.md + HACKS.md → v2.1.201.** Delta v2.1.193–201. Two out-of-band model events dominated, neither in the changelog: **Sonnet 5 shipped (v2.1.197, 30 June)** as the default model in Claude Code for Pro / Team-Standard / Enterprise-seat accounts (Opus 4.8 stays default for Max / Team-Premium / Enterprise-PAYG / API; `sonnet` → Sonnet 5 on the API), and **Fable 5 was restored 2026-07-01** after the US Commerce export-control order was lifted 06-30 — reversing the suspension recorded 2026-06-18. STATE.md gains a Sonnet 5 subsection (native 1M with no `[1m]`/credits, ~967K auto-compact, new tokenizer ~30% token inflation, effort low–max default high, adaptive thinking on, cyber-safeguard refusals as HTTP 200); the Fable 5 section + In-flux entry flip suspended → restored; HACKS §04 caution flips to a restored note. Verified against platform.claude.com model-config + whats-new-sonnet-5 and press (Axios / Al Jazeera / 9to5Mac, 07-01).
- **Changelog delta triage.** Cleared the build/configure/ship filter: `/agents` wizard **removed** (v2.1.198 — STATE bundled-skills line + HACKS §07/§11 corrected; the `claude agents` CLI is unrelated and current); subagents run in background by default (GA, v2.1.198); stacked slash-skill invocations load all leading skills up to 5 (v2.1.199); `"default"` permission mode relabelled "Manual" (v2.1.200); org default models (v2.1.196); `agent_needs_input`/`agent_completed` Notification events (v2.1.198); `autoMode.classifyAllShell` (v2.1.193); `claude_code.assistant_response` OTEL event + the `OTEL_LOG_ASSISTANT_RESPONSES` upgrade footgun (v2.1.193); `/dataviz` bundled skill. HACKS gains `CLAUDE_ENABLE_STREAM_WATCHDOG=0` and `CLAUDE_CODE_AUTO_COMPACT_WINDOW` rows and an updated `CLAUDE_CODE_RETRY_WATCHDOG` row (v2.1.199 lifts the cap of 15, raises the default to 300). The rest of the delta was bug/perf/UX fixes.
- **forge — hooks.md matcher table corrected (cook/serve sweep).** v2.1.195 moved hyphens into the hook-matcher exact-match set: `code-reviewer` / `mcp__brave-search` now match only that literal, not as an unanchored regex. The Matchers table taught the pre-2.1.195 behavior (hyphen fell under "anything else" → regex). Corrected the table row and added the version-safe MCP-wildcard idiom (`mcp__brave-search__.*` catches the whole server; a bare `mcp__brave-search` now exact-matches one literal name). Verified against code.claude.com/docs/en/hooks. Rest of the harness-coupled sweep clean — forge SKILL.md's `Agent(model:opus)` param-rules and nested-subagent triage, skills.md's depth-5 cap, always-on.md, harness-audit's `availableModels`/`enforceAvailableModels` inventory, and ingest all current against the new STATE.
- **Pins not re-tested.** Sonnet 5 is a major model release, but the kitchen's artifacts are earned against Opus 4.8 — unchanged, and still the Max/API default — so the observed failures don't re-test differently just because a different model line got a new version. Left as a user decision: re-run the earning failures against Sonnet 5 only if it's adopted as the working model. CLAUDE.md checked against the delta — no contradictions.

## 2026-06-19 — Opus 4.8 (`claude-opus-4-8`), Claude Code v2.1.177

- **forge reoriented from artifact-discipline to output-first.** Trigger: a head-to-head where Anthropic's eval-driven skill-creator produced a better research skill than forge, and a review (mine) that scored forge's "win" on artifact-properties — token leanness, an over-fire guard, CHANGELOG hygiene — rather than on the quality of work the skill produces. Claudia's call, and it's right: the only thing worth measuring is the OUTPUT — what the skill makes an agent do, judged against the best practitioner in the domain — and forge had drifted into optimizing *itself* (surfaces, budgets, triggering, lifecycle) above that. The drift was visible inside forge's own `depth-eval.js`: it defines a genuine expertise/"depth" axis, then its caveats tell the reader to "treat craft/anatomy as the trustworthy machine signal" when the depth judge gets hard, and flag an "anatomy regression" when a guided skill trades polish for substance. That is the inversion, in forge's own code.
  - **SKILL.md opening rewritten** to lead with the thesis (a skill transplants expertise; its only measure is the output against a world-class bar; everything else is plumbing), the expertise-acquisition mandate (if you can't describe exemplary output, you aren't qualified yet — go research the practice / write an exemplar first), and a fallibility stance (forge is a working draft, not the authority; defer to whatever produces better work — e.g. a rigorous skill-creator — and cut forge rules that don't serve output). The three bars reordered so "encodes expertise / beats the default" is heaviest, not third; additive-vs-transformative reframed as about the work, not the token bill.
  - **Eval bullet (Skills section) rewritten** so the verdict is output-against-exemplar, not fire/no-fire: establish the exemplar first (Anthropic evals guidance — a good task is one two domain experts grade the same, proven solvable by a reference solution), then grade whether the output closes the gap. trigger/invocation evals demoted to secondary hygiene; depth-eval is the grade; when the depth judge is weak in-domain, calibrate to exemplar/human, never retreat to anatomy.
  - **`depth-eval.js` caveats #2/#3 rewritten** to match: depth stays the verdict and gets calibrated to ground truth; craft/anatomy are hygiene, not the grade ("a skill can be flawlessly-shaped and still encode floor"); the anatomy-vs-substance trade is usually correct and only a failure if it breaks firing. Also sharpened "Distill, don't transcribe" to keep the expert's *judgment* (the fork-call, the trap) over a followable procedure.
  - **External grounding:** Anthropic, *Demystifying evals for AI agents* (calibrate LLM judges to human experts; build reference solutions; the two-experts-agree task bar); the 2026 LLM-as-judge literature on ceiling effects and verbosity/self-preference bias, whose prescribed fix is domain-calibrated rubrics anchored to expert ground truth — the opposite of forge's retreat to easy metrics.
  - **CLAUDE.md reflection ritual fixed too.** "New artifacts are feedback for the forge" asked only about the forge's *coverage* (friction, missing triage/stance/references, anti-patterns) — never whether the artifact's output was good, embodying the same inward bias. Added a leading question that makes output-against-an-expert-bar the first thing assessed after delivery. Deliberately did NOT copy the output-first philosophy or a fallibility line into CLAUDE.md — the former now lives in forge's body (loads when working on forge), the latter is already CLAUDE.md line 1; duplicating across scopes is the anti-pattern.
  - **Re-test / sunset:** on the next major model release, re-run `depth-eval.js` across one model-strong and one model-weak domain. If the output-first framing no longer changes what gets built (the model self-acquires the expert bar unprompted), trim it. Watch-item: does the expertise-acquisition mandate push forge to over-research trivial artifacts? If so, add a proportionality clause (acquire to the depth the artifact's stakes warrant). Not yet re-validated against a fresh build — next move is to rebuild the research skill under the new framing and run the depth panel against the skill-creator output as the exemplar.

## 2026-06-18 — Opus 4.8 (`claude-opus-4-8`), Claude Code v2.1.177 (running binary; STATE.md/CHANGELOG track the v2.1.179 docs)

- **worklog + worklog-minor folded into `serve/` from the standalone `claudialnathan/worklog` repo.** That repo duplicated the kitchen's whole connective tissue — its own CHANGELOG, CLAUDE.md, marketplace, `hooks/`, and a separate path-keyed memory store — for one project-memory tool that already shares the kitchen's authoring discipline (its own CLAUDE.md: SKILL.md is the product, the description is the trigger, the body is a standing cost, don't re-grow it). No external users; the standalone repo is being deleted. Brought in: both `SKILL.md` and `worklog/references/setup.md`. Left behind as repo formalities: `CONVENTION.md` (the `.worklog/` format spec, already inlined verbatim in both skills), `hooks/inject-brief.sh` (the SessionStart BRIEF injector), `internal/`, LICENSE/SECURITY/.github, and the marketplace `version` field (kitchen is commit-SHA; `preship-check` gate 6 forbids it). worklog-minor's `CONVENTION.md`-as-source-of-truth and SessionStart-hook lines were rewritten to point at the inlined format and the worklog skill, so each skill stands alone.
- **Placement and shape.** Put in `serve` (applied discipline) as two skills, not a third marketplace plugin — it's a skill with reference files, not a plugin-shaped unit. worklog-minor kept as a *separate skill* rather than a mode of worklog or a slash command: the fast append exists precisely to avoid loading worklog's ~240-line body for a one-line jot, so folding it in would defeat its reason to exist, and a pure command would lose the natural-language trigger ("jot this for the next agent"). The two are bundled siblings — worklog names the fast path, worklog-minor names the full skill for the spec — and each runs standalone. `serve` `marketplace.json` + `plugin.json` descriptions extended with worklog. Pinned Opus 4.8, 2026-06-18, v2.1.177. User-scope `~/.claude/skills/worklog` repointed from the soon-deleted standalone repo into the kitchen and `worklog-minor` added; durable cross-repo propagation is the `serve@claudia` plugin after the next push + `/plugin update`.
- **forge — `evals/invocation-eval.js` added (LIVE trigger ground truth).** `trigger-eval.js` is a text proxy — a judge reasons about a listing entry and *guesses* fire/no-fire — and its own caveat #1 named the missing higher-fidelity test: a real session, the skill installed, the firing *observed* (skill-creator's `run_eval.py` drives `claude -p` for that). Built it. The harness copies the **real SKILL.md** into a throwaway project, loads it via `--add-dir`, drives one `claude -p --output-format stream-json` per query, and detects the candidate by an exact `Skill` tool_use `input.skill` match — a *different* skill firing reads as a non-fire. Reports recall (should-fire), specificity (should-stay), accuracy. Run with `node`, **not** the Workflow tool — it shells out and writes temp files, which the Workflow sandbox forbids; the evals dir's first subprocess-driven harness (the other two are Workflow scripts). Pinned Opus 4.8, 2026-06-18, v2.1.177.
- **Two honest improvements over `run_eval.py`.** It tests the actual artifact (real name + description, with an optional live-swapped description via `--description` for an A/B), not a synthesized command-file stub; and exact-skill matching turns the real competing listing into a feature rather than noise — the proxy's caveat #2 (single skill, no competition) is gone.
- **Mechanism verified live, not asserted.** Probed v2.1.177 directly: a skill loaded via `--add-dir` shows up in the session skill listing and, on the configured model (Opus 4.8), fires as `{"name":"Skill","input":{"skill":"<name>"}}` — the exact shape `run_eval.py` assumes, confirmed still current. A clean-room config dir (`CLAUDE_CONFIG_DIR`) was ruled out: a fresh one carries no credentials (`apiKeySource:none` → "Not logged in"), so the harness runs under real auth and isolates via the temp project instead (caveat 1). Haiku 4.5 declined a strongly-matching query — small models under-invoke; the default is the shipped model (caveat 2).
- **A detection bug, caught by the never-ship-unrun rule.** First validation run reported a no-fire on a skill the raw probe had just fired: the parser treated an opening prose sentence as a decline and killed the subprocess before the model's `Skill` call. Models preface tool calls with text — fixed to fire on a matching `Skill` call and decline only at `end_turn`. Re-validated: postmortem skill recall=1 (PASS); saltintesta on a Python task correctly stayed (PASS).
- **Proxy and live can disagree — the reason the harness exists.** In the (n=1, cwd-collision-confounded) validation, saltintesta did *not* fire on "write a blog post" in a real `-p` turn — the model wrote it inline — yet `trigger-eval`'s proxy had scored saltintesta full recall. Not a verdict on saltintesta (needs a real run from a minimal repo), but exactly the blind spot a judge-as-proxy cannot see: "would you load this?" (yes) ≠ "handed the task, did the model load it?" (often no). Logged for a proper sweep.
- **Wired + resolved.** forge SKILL.md and references/skills.md *Eval* now present three harnesses as a cheap-proxy → live-ground-truth → quality ladder; `trigger-eval.js` caveat #1 now points at `invocation-eval.js` as the realized ground truth; gitignored TODO item #2 (proxy → live invocation) closed.
- **Forge learning.** The Eval guidance had collapsed "does it trigger" into one proxy harness; the live build shows triggering has two fidelities that can disagree, and that a model handling a matching prompt inline is a real failure only the live run catches — folded into the Eval bullet. The evals dir now mixes Workflow scripts with a node-run subprocess harness; the run-with-`node`-not-Workflow distinction is a footgun, documented in the header and the skill.
- **First live measurement (saltintesta) — the proxy over-read recall.** Ran invocation-eval against saltintesta from a no-collision cwd (real auth; ~148 global skills competing — a conservative, heavy-competition setting), 12 queries × 3 reps. Live **recall 0.63, specificity 1.0, accuracy 0.75** — against `trigger-eval`'s proxy verdict of *full recall*. The misses cluster: "draft a short essay", "tighten this writing", and "make it less AI-sounding" fired 1.0, but "write a conference-talk opening", "rewrite the README intro", and "edit this ADR summary" fired **0/3** — the model just does those inline. Specificity was perfect (no near-miss fired). Confounds logged: heavy competition (a docs/README skill may steal those queries), n=3, and `-p` one-shot ≠ interactive — so 0.63 is a floor, not a fixed number. The headline holds regardless: a judge guessing "would you load this?" (yes) is not the model, handed the task, choosing to (often no) — the disagreement the harness was built to catch.
- **Live A/B verdict — enumeration earns nothing (confirmed), but not for the proxy's reason.** Re-ran saltintesta both ways from the same heavy-competition cwd, 12×3 each: terse current description (recall 0.63) vs one naming READMEs, ADRs, talks, and "rewrite this intro" explicitly (recall 0.50). Enrichment did not lift recall (0.50 vs 0.63 is inside n=3 noise), and the decisive part: the failing triggers stayed dead — "rewrite the README intro" and "edit this ADR summary" fired **0/3 in *both* arms**, with READMEs and ADRs named in Arm B's description. The model declines saltintesta for inline-editable edit tasks *regardless of wording*. This agrees with the earlier fold-`when_to_use` decision on **action** (don't enumerate) but corrects the **reason**: not "the description already covers these at full recall" (the proxy's claim) but "no wording triggers these — the model just does the edit." Specificity stayed 1.0 across all three runs.
- **Two harness limits surfaced — both fixed the same session.** invocation-eval now resolves a rich per-run outcome (*fired* / *stolen by a named skill* / *inline* / *error*) instead of a bare boolean, which closes three holes a `0` used to hide: (a) CLI/auth failures and timeouts are excluded from rates, not miscounted as no-fires; (b) a miss is labeled, so "the model handled it inline" is separated from "a competing skill stole it"; (c) each run reads the `init` event to confirm the candidate actually loaded — a silent `--add-dir` failure would otherwise read as "never fires" — and to report the competing-skill count. The output shows `fired=k/n` (n visible) and the alternative per miss; reps left at 3 (surfacing n beats forcing token cost — that's the n-noise answer). Validated reps-1 amid 149 competing skills: every saltintesta miss was labeled `inline`, none `lost to` another skill — first evidence the README/ADR/talk misses are genuine inline-handling, not competitor-stealing. Loader-safe, `preship-check` green; pin unchanged (same model/version/day).

## 2026-06-17 — Opus 4.8 (`claude-opus-4-8`), Claude Code v2.1.179 (Fable 5 withdrawn)

- **Fable 5 + Mythos 5 suspended 2026-06-12 — out-of-band correction.** A US Commerce Department export-control order forced both models offline; Anthropic complied globally (API, AWS, Foundry), unable to isolate foreign nationals in real time. Indefinite, contested, no restoration timeline (reported across InfoQ / Snyk / MarkTechPost, 2026-06-13; trigger was a jailbreak toward vulnerability identification). **The event never reached the Claude Code changelog** — a legal/availability action, not a version feature — so the 06-12 and 06-15 state bumps, which read the changelog, missed it, and STATE.md + HACKS.md kept presenting Fable 5 as the live "most capable model in Claude Code." Corrected: STATE.md Fable 5 section and In-flux entry now lead with the suspension; HACKS.md §04 gains a suspension caution; datelines moved to 06-17.
- **Forge lesson — pins must survive withdrawal, not just succession; the bump ritual must catch availability events.** Model pinning assumed models move *forward* (earn against X → re-test when its successor ships). A withdrawal breaks the trigger: forge, design-engineer, and harness-audit pin to `Fable 5`, and speed-daemon + quality-audit carry a Fable 5 revision note — five re-test triggers now point at an unreachable model. And `state-bump` is changelog-driven, but model availability moves out of band. Two gaps, now closed: (1) the five artifacts re-pinned to Opus 4.8 (the durable default of that window), and forge + CLAUDE.md now teach that a pin survives withdrawal, not just succession; (2) state-bump Step 0 gains an out-of-band model-availability check, since the changelog won't carry a recall.
- **Cross-tool review fixes.** Trigger: an external multi-tool review of the repo. Prose typos corrected in saltintesta (`alwats`, `rudimenatry`, `ocnvention`, `it's`→`its`) and speed-daemon (`A`→`An`) — the prose-quality skills can least afford them. `bin/preship-check` comment #1 dropped a stale CLAUDE.md quote ("two real failures already") removed in the 06-12 rewrite — the repo's own don't-restate-stale-state law, applied to a comment. `serve` `plugin.json` description aligned with `marketplace.json` (added `flavored-md`). HACKS.md §13 label `12 THE WHEEL` → `13`, and a malformed `<pre>` close tag fixed.
- **Verified clean.** Skill descriptions are *not* deferred in Claude Code (eager listing confirmed against the v2.1.179 changelog and a live session) — harness-audit's "descriptions load every turn" cost model holds; the external claim that skills are search-deferred was a different harness. `/code-review` vs `/simplify` split (renamed v2.1.147, `/simplify` restored as a `--fix` alias v2.1.152) matches STATE.md as written. The `/v1/dreams` surface in HACKS.md §09 verified accurate against platform.claude.com docs (endpoint, 1–100 sessions, both beta headers, 4,096-char instructions, model list) — HACKS.md's channels (iMessage `chat.db`, sender allowlist) and deep-links (`claude-cli://`, `q` 5k cap, `cwd`>`repo`, `disableDeepLinkRegistration`) rows verified too. One real error, fixed: the dreams escape-hatch cited a "2,000-store cap"; the documented limit is 2,000 *memories per store* (HACKS §01/§09 corrected).
- **shadcn-tailwind pinned + stack-keyed sunset.** Previously unpinned, and the most version-fragile applied skill. Pinned (Opus 4.8, 2026-06-17, v2.1.176) — but its real re-test trigger is a *stack* major (Tailwind v5, shadcn 5, or a Base UI breaking change), not a Claude model release; on that trigger, re-verify the token mechanics (oklch/rem) and the `render`-not-`asChild` + `data-slot` conventions against live docs.
- **STATE.md + HACKS.md → v2.1.179.** Changelog delta v2.1.177–179 was almost entirely bug/perf fixes; three entries cleared the build/configure/ship filter — `Tool(param:value)` parameter-level permission rules and nested-`.claude/` proximity resolution (both v2.1.178) added to STATE.md *Other small but useful*, and the `availableModels` line amended with v2.1.177's bypass-closing (`ANTHROPIC_DEFAULT_*_MODEL` + `/fast`); matching HACKS §03/§04 rows, datelines re-pinned. The new out-of-band availability check confirmed Fable 5 still suspended. Not a model release — no pin re-test triggered; CLAUDE.md clean against the delta.
- **Sweep (Step 7) — both candidates applied.** `Tool(param:value)` (v2.1.178) is a new enforcement lever: forge's triage row for `permissions.deny` now notes command *and* parameter matching (`Bash(rm *)`, `Agent(model:opus)`) — a block that looks conditional often needs no hook; and harness-audit's enforcement-parity check (Step 3) now lists permission rules, param matches included, as a mechanism a prose rule can map to.
- **forge — `evals/trigger-eval.js` added (trigger-accuracy harness).** The forge measured output quality (`depth-eval.js`, blind A/B panel) but never trigger accuracy — the under/over-fire failure it names as central. New sibling: over a labeled query set (should-fire vs near-miss), a judge rules invoke-or-not per query × reps, and the script A/Bs a listing's `description` alone against `description + when_to_use` to size what open-spec consumers lose (agentskills.io defines no `when_to_use` and caps `description` at 1,024 chars — verified live 2026-06-17). Pinned Opus 4.8, 2026-06-17, v2.1.179; wired into forge SKILL.md (the scale-to-stakes line names both harnesses) and references/skills.md (Listing-and-triggering, Eval). Header documents the proxy caveat: it scores the description's discriminating power, not a live invocation.
- **`when_to_use` rarely earns its place — measured, not asserted.** First trigger-eval run (saltintesta, 12 queries × 3 reps): `description` alone scored full recall — *including* the four queries whose trigger words (`edit`, `tighten`, README, ADR) appear only in `when_to_use` — and full specificity on near-misses; zero lift from the field (descOnly == descPlusWtu == 1.0 throughout). Semantic matching covers a good description's neighborhood, so phrase-enumeration in `when_to_use` duplicates it; skills.md frontmatter table sharpened to say so. Acted on: since the field earns nothing, `when_to_use` was folded into `description` and **deleted across all nine cook + serve skills** — zero change to the Claude Code listing (the two were already concatenated there), a strict gain on Cursor/Codex (the open spec has no `when_to_use`, so they drop it). `design-engineer`'s description was quoted in passing, fixing an unquoted colon-space that breaks strict (PyYAML-class) parsers. The deferred all-`serve/` trigger-eval sweep (gitignored `TODO.md`) stands as later confirmation, not a precondition.
- **`harness-targets` dropped; cook skills made portable.** `harness-targets: [claude]` was a `bin/sync-cross-tool`-only convention — inert in Claude Code (unrecognized field, silently ignored), absent from the agentskills.io open spec, and redundant with the cook/serve split (only cook skills carried it, all `[claude]`). Removed from forge, harness-audit, and ingest; with the field absent the sync script defaults them to all three tools, so they now distribute to Cursor and Codex like the serve skills (forge/harness-audit are Claude-Code-specific in *content*, but *distribution* now matches the open-format default; the script still honors an explicit `harness-targets` override if a future skill needs it). The forge frontmatter trap that used `harness-targets` as its don't-copy example reworked to use `paths:`. Cross-tool symlinks synced (+3 each into ~/.cursor, ~/.codex).
- **Pending.** The 2026-05-31 skill-forge depth-eval (+0.89 craft) re-baseline now runs on Opus 4.8.

## 2026-06-15 — Opus 4.8 (`claude-opus-4-8`), Claude Code v2.1.176

- **STATE.md + HACKS.md → v2.1.176.** Changelog delta v2.1.172–176 (no v2.1.171) was almost entirely bug/perf fixes (background sessions, `/model` picker, Bedrock, Remote Control); three entries cleared STATE.md's build/configure/ship filter. STATE.md: nested subagents (v2.1.172) and the `availableModels`+`enforceAvailableModels` managed allowlist (v2.1.175) added under *Other small but useful*; the Fable 5 model-ID line corrected — 1M is default and a `[1m]` suffix is now stripped (v2.1.173), so the ID is `claude-fable-5`. HACKS.md: nested-subagents row (§07) and the managed model-allowlist row (§04) added; all three datelines re-pinned. CLAUDE.md checked against the delta — no contradictions, and a maintenance bump (not a model release) leaves the pin re-test trigger dormant.
- **forge — nested subagents threaded through.** The cook/serve sweep (run against the freshly-bumped STATE.md as current truth) found the forge silent on a v2.1.172 capability: a subagent whose `tools` include `Agent` (the default) spawns its own subagents. Mechanics added to `references/skills.md` Frontmatter traps, beside the `tools:`-vs-`allowed-tools:` trap — omit `Agent` / `disallowedTools: [Agent]` to keep a subagent leaf-only; foreground self-limits, background caps at depth 5; a fork still can't spawn a fork. Judgment added to SKILL.md: the orchestration triage row and the Workflows "surface line" now seat a nested subagent between a prose-dispatched skill and the workflow runtime — a single delegated task that self-splits (reviewer → a verifier per finding) needs only a nested subagent, while the runtime stays earned for large fan-out, branching, or reproducible runs. Verified against code.claude.com/docs/en/sub-agents.
- **harness-audit — managed model/version governance in enforcement parity.** Same sweep: the enforcement-parity check (Step 3) and the settings inventory (Step 1) named hooks and CI gates as the mechanisms a prose rule must map to, but not managed settings. A "we standardize on model X" line is enforced only by `availableModels`/`enforceAvailableModels` (v2.1.175), not prose — added as a mechanism type with that example, and the model/version-policy managed settings added to the inventory note. Pin left at the core earning (Fable 5, v2.1.170); this lever's provenance is here.
- **Held.** A harness-audit roster note (flag which roster entries carry `Agent` and so can fan out) was considered and dropped: nesting is a *runtime* fan-out cost, and that skill's discipline is standing per-session cost — out of its frame. ingest audited and clean: its flat one-subagent-per-source fan-out has no subtask for nesting to fill.

## 2026-06-12 — Fable 5 (`claude-fable-5`), Claude Code v2.1.170

- **The five artifact forges consolidated into `forge` (cook).** Trigger: the Fable 5 re-test ledger (2026-06-10, pending) plus a harness review against the repo's own laws. skill-forge, hook-forge, rule-forge, claude-md-forge, and workflow-forge shared one skeleton (name the failure → triage → steps → checklist) with the triage ladder duplicated across all five, and the meta layer had grown to ~65% of the repo's markdown. The consolidation keeps two kinds of content and cuts the third: the **stance** (earned origin, nameable attention shift, additive-vs-transformative, the pin/changelog/sunset law) lives in `forge/SKILL.md`; the **empirical mechanics** live in three references (`skills.md`, `hooks.md`, `always-on.md` — listing caps, compaction budgets, the loader footgun, hook events and exit codes, CLAUDE.md ceilings, rule glob semantics all preserved); the **process scaffolding** (five Step 0s, per-surface closing checklists, worked-example bodies, anti-pattern catalogs) deleted as restating what the current model does unaided. Roughly 5,300 lines → 600.
- **workflow-forge — DELETED (re-test verdict).** Its API and pattern content (pipeline-vs-parallel, the barrier smell test, budget scaling, adversarial verification, the anti-pattern list) is now carried nearly verbatim by the Workflow tool's own in-session description, which is also more current. Residue folded into forge's Workflows section: the surface ladder, the packaging caveat (skill-folder packaging is blog-asserted; `.claude/workflows/` is canonical), the opt-in rule. The worked example moved to `ingest/examples/ingest-fanout.workflow.mjs` — still authored-but-never-run; a template, not a script.
- **skill-forge / hook-forge / rule-forge / claude-md-forge — content verdicts.** hook-forge and claude-md-forge carried the highest fact density (events/exit-code semantics; ceilings/filters/voice) — preserved nearly whole in the references. rule-forge's distinct content (condition-shaped bullets, respect-the-framework preamble, discovery-instruction pattern, rule-vs-skill invocability) preserved compact. skill-forge's pedagogy (eight kind templates, three worked examples, twelve reference files) cut hardest; its theory — additive-vs-transformative, the depth gate, the blast-radius rule — *is* the new forge's opening stance. Caveat: the 2026-05-31 depth-eval measured the pre-consolidation guidance (+0.89 craft); `forge/evals/depth-eval.js` (paths updated) should re-run to re-baseline.
- **design-engineer — blast-radius compliance + absorption.** The body deferred whole topic areas to `emil-design-eng` and `web-design-guidelines` ("owns those — cite that skill, don't reinvent"), both personal-scope skills not shipped with the plugin — a violation of the blast-radius rule recorded 2026-06-10. The load-bearing animation craft is now absorbed in `references/animation-craft.md` (easing tree and custom-curve tokens, duration tables, springs and interruptibility, enter/exit discipline including asymmetric timing, clip-path patterns, gesture mechanics, performance internals including the Motion-shorthand hardware-acceleration trap, reduced-motion-means-gentler); both external skills demoted to soft, optional, one-directional pointers. New "Name it precisely" vocabulary section in `references/taste.md`, adapted from index.how's design vocabulary (optical vs mathematical centering, affordance, filled-vs-outlined as state, voice vs tone, front-loading, measure) — grounds state-the-reason in checkable nouns. Fixed a stray `$$` typo in the SKILL.md transform-origin example. README reframed: bundled vs absorbed vs optional, replacing a flat SKILL DEPS list that read as install requirements.
- **Rename fixes.** ingest now hands off to `/forge` with a surface recommendation (was per-forge slash commands); quality-audit's hook pointer and README corrected (hook-forge had been listed as a hard dep on the strength of one optional pointer); CLAUDE.md's naming-collision and footgun-pointer paragraphs updated; cook `plugin.json` description updated.
- **Sunset for `forge`:** the same law it teaches — on the next major model release, re-test whether a skill-withheld session triages surfaces correctly and carries the mechanics unaided; the empirical references are expected to outlive the stance.
- **CLAUDE.md audited (forge always-on discipline, verify-first).** Verdicts: intro and governance rewritten for the singular forge ("meta-forges" → "the meta layer"; feedback-section bullets now reference the forge's actual structure: triage, stance, references); the artifact-is-not-the-bibliography rule added beside the conversation-residue rule; "run preship before commits" replaced with the verified enforcement fact (committed PreToolUse hook blocks failing commits); the publishing-footgun's pointer to a recovery playbook cut — it chained through `CLAUDE.local.md` to `FYI.md`, which does not exist on disk (gitignored, so never committed; flagged to the user rather than reconstructed). Everything else verified and kept: framing caveat, pinning discipline, both footguns, naming collisions, dates. `guides/`, STATE.md, HACKS.md, the hook wiring, and `harness-targets` gating on all three cook skills checked against the filesystem.
- **Source-residue pass (design-engineer + forge).** Rule earned, generalizing no-conversation-residue: a skill is standing instructions to the agent, not an annotated bibliography. Person attributions ("per X", "(X's essay)"), quote collections, "Further reading" lists of books/courses/author blogs, and human-addressed training advice (develop-your-taste mechanics, prompting tips, "review with fresh eyes over months") removed from design-engineer's body and references. Kept: canonical doc URLs the agent can verify against mid-task (motion.dev, base-ui.com), and soft "if installed" pointers to optional skills. `taste.md` rewritten agent-facing (~265 → ~150 lines): the what-AI-can't-do composite became "The limits of your unaided taste" in second person (median-motion default, novelty blindness on high-frequency surfaces, don't manufacture conviction where no stated reason exists, scaffold ≠ finished UI, large asks as sequential narrow passes); quotes that encoded rules were folded into the rules; the rest cut. Attribution now lives only in skill READMEs (src links) and here — for the record: state-the-reason / articulate-before-revealing / animation craft trace to Emil Kowalski's writing; frequency × novelty, novelty-as-accent, and the depth toolkit to Rauno Freiberg's craft series; the AI-collaboration limits to Jakub Krehel; the precision vocabulary to index.how; the CLAUDE.md rule-compliance ceilings in `forge/references/always-on.md` to Addy Osmani's May 2026 study. saltintesta untouched — its Paul Graham reference is the namesake concept in the user's own prose.

## 2026-06-11 — Fable 5 (`claude-fable-5`), Claude Code v2.1.170

- **Ingested `addyosmani/web-quality-skills`** (all six skills read in parallel: performance, core-web-vitals, accessibility, seo, best-practices, web-quality-audit; MIT). Treated as authoritative on web-quality substance. Verdict: near-zero overlap with speed-daemon — that repo works at the page/network/main-thread layer, this fleet's gap was exactly there. Nothing contradicts the optimistic-UI default; core-web-vitals' "visual feedback provided immediately" is the same instinct one layer down. Every adoption below was adapted in place; no cross-skill dependencies — the source skills appear only as optional depth references in quality-audit's routing table.
- **speed-daemon — fourth leg: local heavy work.** The network isn't the only render gate; a long main-thread task is the same failure with no spinner to blame. New pattern: cheap state echo → yield (`scheduler.yield()`, `setTimeout(0)` fallback with its priority caveat; `useTransition` in React) → heavy work after the paint → analytics to `requestIdleCallback`. INP gives "feels slow" a number (≤ 200ms at p75; input < 50ms / processing < 100ms / presentation < 50ms); the `web-vitals` attribution build named for field debugging. Pre-ship check and the re-test protocol gain the expensive-input shape (filter/search over a large list). Description and `when_to_use` extended to sluggish-input and INP complaints.
- **quality-audit — two new dimensions plus WCAG 2.2 sharpening.** Web vitals (code level): unsized media, LCP image priority, font-display with metric-matched fallbacks, third-party script strategy and facades, above-viewport injection, virtualization, LCP content in initial HTML — framed as the code shapes that decide the field numbers, since a code audit can't measure p75. Security & best practices: `npm audit` (vulnerable deps = P0), raw HTML sinks, CSP/HSTS/header set, SRI on uncontrolled CDN assets (polyfill.io 2024), secure cookies, hidden source maps, deprecated APIs, passive listeners, global error handlers. Accessibility fallback gains the numbers: 4.5:1 / 3:1 contrast, 24px target floor (SC 2.5.8), focus-not-obscured (SC 2.4.11), single-pointer drag alternative (SC 2.5.7), focus-indicator contrast.
- **design-engineer — citation precision.** Hit-area item now states the real WCAG 2.2 floors (24px AA per SC 2.5.8; 44px AAA per SC 2.5.5; the skill's 40px floor clears AA) — it previously cited SC 2.5.5 for a 40px floor that matches no actual criterion. Scroll-margin item extended from anchored sections to keyboard focus under sticky bars (SC 2.4.11), with the global `:focus { scroll-margin }` rule in `references/polish.md`.
- **Held from the same source:** no SEO skill (the personal-scope geo/seo fleet already owns that ground — triage gap not real) and no standalone accessibility or security skills (their high-leverage content folded into quality-audit's fallback checklists instead).

## 2026-06-10 — Fable 5 (`claude-fable-5`), Claude Code v2.1.170

- **Convention: one-line pins.** Inline `Earned against` comments reduced to a single line across all skills; their history migrated to this file. `bin/preship-check` warns when a pin outgrows one line. Artifacts never narrate the sessions that produced them.
- **All cook/serve frontmatter descriptions rewritten** for listing effectiveness: job + scope + strongest triggers in `description`; `when_to_use` as one compact line (bullet lists render as run-on text in the skill listing). Model-visible total 12,007 → 9,556 chars; no distinct trigger keyword dropped; duplication and body-restatement removed.
- **skill-forge** — "Description first" gains rule 5: the 1,536-char cap is a truncation guard, not a target (job + scope + triggers lands at 600–900 chars); a fleet of near-cap descriptions overflows the shared ~1%-of-context listing budget, evicting least-used descriptions — the silent under-triggering the padding was meant to prevent. Never restate body discipline in a description; for `paths:` skills the body auto-loads when the rules apply. Earned from this fleet's own drift (two skills within 35 chars of truncation).
- **speed-daemon** — gained `paths:` globs; its `when_to_use` claimed auto-load on reactive UI files but no globs existed.
- **Fleet frontmatter pass: request classes over phrase lists.** Triggering is semantic, not lexical — the model reads the listing and matches meaning — so every cook/serve `when_to_use` was rewritten from quoted-phrase lists ("write something on X", "draft a piece about Y") to a statement of the request class plus only the anchors the class doesn't imply (skill names, goal phrasings like "make this less AI-sounding"). Phrase lists sample the center of the request distribution while misses happen at the boundary, and an enumerated list reads as exhaustive — the unlisted case looks out of scope. Same pass: em-dash connectors and cross-skill routing clauses ("composes with", "pairs with", "NOT for X, use Y") dropped from descriptions; the deference notes already live in the bodies. skill-forge "Description first" rule 2 and references/triggering.md now teach the class-first shape; "Distill from source material" gained the blast-radius-of-one rule (ingesting into a skill never adds content or back-references to a neighboring skill).
- **saltintesta / flavored-md — cross-coupling reverted.** A 2026-06-09 ingest had woven the two skills into each other: register/layout paragraphs added to saltintesta's body, flavored-md leaning on saltintesta for its cut-the-fluff half, and (2026-06-10) a rewritten saltintesta description referencing flavored-md. All reverted: saltintesta's original description restored, the register/layout paragraphs removed (that ground lives in flavored-md's "The reader scans" and "Cut the fluff" sections), flavored-md made self-contained with a single optional "if saltintesta is available" pointer. Rule earned: ingesting material into one skill never adds content or references to a neighboring skill; cross-references are soft, optional, and one-directional.
- **harness-audit (cook) — new.** Audits a Claude Code setup end-to-end: inventory the standing surfaces → quantify per-session token cost → four checks (self-consistency, duplication across scopes, enforcement parity, scope discipline) → quantified triage. First worked example: this repo's own setup, which surfaced ~4,600 tokens/session of personal-skill descriptions, the kitchen's 12 skills double-listed (project symlinks + user-scope plugins), a loaded file describing itself as not loaded, and an "always run preship" claim with no enforcement. Sunset: on the next major model release, re-test whether a skill-withheld session inventories scopes, quantifies cost, and checks claims-vs-behavior unaided; if so, delete.
- **Repo machinery** — `.claude/hooks/preship-gate.sh` + committed `.claude/settings.json`: PreToolUse hook blocks `git commit` when `bin/preship-check` fails. `CLAUDE.local.md` slimmed to session-relevant facts; install/troubleshooting playbooks moved to the unloaded `FYI.md`. `bin/preship-check` gains an advisory description-length warning (>1,100 chars, skipping `disable-model-invocation` skills) and the pin-length warning.
- **STATE.md → v2.1.170** — Fable 5 section (opt-in via `/model fable`, `best` alias, effort default `high`, thinking not disableable, classifier fallback to Opus incl. workspace-context triggers, 1M always-on via API, pricing/rollout); In-flux entry for the time-boxed subscription window. **HACKS.md re-pinned to v2.1.170** — Fable classifier-fallback gotcha, `/model best`, `--safe-mode`, `fallbackModel`, `disableBundledSkills`, `/cd`, and the `MAX_THINKING_TOKENS=0` Fable exception.
- **Re-test ledger opened.** The Fable 5 release triggers re-tests of every pre-2026-06-09 pin: speed-daemon (rebuild mutation shapes fresh; delete if optimistic-by-default), workflow-forge (do laziness/drift still justify the surface? worked example never ran end-to-end), flavored-md (GFM form-choice and traps unaided?), quality-audit (stack-aware multi-lens audit unaided?), ingest (does quote-grounding still need scaffolding?), and the 2026-05-30 forge craft passes. Status: **pending**.

## 2026-06-09 — Opus 4.8, v2.1.165

- **flavored-md (serve) — new** (renamed from `github-flavored-md` the same day; subject unchanged). GFM form-matching: table / alert / collapsible details / task list / footnote / mermaid chosen by the content's job; GitHub-specific traps verified against the GFM spec and GitHub docs at authoring, not recall (exact alert syntax; color swatches render only in issues/PRs, not repo files; auto-generated heading anchors break on emoji/punctuation). Reviewed against an external markdown-skills repo for overlap — out of scope, no changes. Sunset: re-test form-choice and traps on the next major model release; delete if handled unaided.
- **saltintesta** — antipattern recorded: the artifact is not the conversation. Files written mid-conversation absorb conversational residue (rebuttals, reader-addressed justifications); earned from a reference doc that had absorbed exactly that.

## 2026-06-08 — Opus 4.8, v2.1.165

- **workflow-forge (cook) — new.** Designs dynamic-workflow orchestration scripts. Two facts carry different confidence: the Workflow runtime API (`agent`/`parallel`/`pipeline`/`phase`, concurrency caps, no filesystem) is documented at code.claude.com/docs/en/workflows; packaging a workflow inside a skill is blog-asserted only — the documented reuse path is `.claude/workflows/` as a `/command`. The worked example was authored against the runtime but not run end-to-end. Sunset: when workflows leave research preview or on the next major model release, re-verify the API, confirm or demote the skill-packaging path, and re-test whether the model still goes lazy / drifts on big multi-part tasks unaided — that failure justifies the surface.
- **claude-md-forge** — functional CLAUDE.md↔FYI boundary: a behavioral precondition the agent acts on during normal work belongs in CLAUDE.md even when it is publish-flow; only playbook detail stays out. MCP anti-pattern sharpened: a connected tool documents its own use (server instructions + per-tool descriptions), so CLAUDE.md should not restate tool usage.
- **ingest** — pasted-source branch (material already in context is extracted inline; subagents only for what must be fetched); check-for-existing-brief step before writing, after a run nearly duplicated a same-day brief.
- **skill-forge** — triage note: scheduling is orthogonal to surface choice. A scheduled or event-fired job is a Routine (`/schedule`) running a skill, not a self-scheduling skill.
- **Repo** — plugins switched to commit-SHA versioning: `version` fields removed from both `plugin.json`s and the marketplace entries; `bin/preship-check` enforces their absence. A pinned version string had frozen the install cache and silently blocked propagation for weeks.

## 2026-06-06 — Opus 4.8, v2.1.165

- **quality-audit (serve) — new.** Stack-aware, read-only quality-audit dispatcher: detect the stack from package.json → run real verification (lint, typecheck, build) → route dimensional lenses → one P0/P1/P2 report. Resilient to absent routed skills (fallback checklists in `references/dimensions.md`). Sunset: re-test on the next major model release whether the model runs a stack-aware multi-lens audit unaided; the routing + honesty + triage spine is expected to outlive the dimension fallbacks.
- **skill-forge** — frontmatter `name` guidance corrected from "usually omit" to "include it": directory-name inference is Claude-Code-only; the open spec, cross-tool consumers, and `bin/preship-check` all require the field.

## 2026-05-31 — Opus 4.8, v2.1.156

- **skill-forge** — A/B depth-eval across three domains with a blind judge panel (artifacts in `evals/`): the guidance lifts craft +0.89, anatomy +0.44, depth +0.33. Two regressions found and fixed: verification became an explicit fetch-and-cite step after a recall-based "verify" shipped a deprecated API; portability guidance added against copying this forge's own Claude-gated frontmatter onto portable skills. Source-verification of the model-weak eval domain added "cite honestly" to the depth gate — correct expert facts can still carry citation-shaped fabrication. Also added: a keep-signal counterpart to the sunset audit, and proportional cost-gated verification tiers.

## 2026-05-30 — Opus 4.8, v2.1.156

- **Cross-forge craft audit.** skill-forge re-architected around two co-equal pillars (expert subject + harness anatomy) after review found it over-taught anatomy. hook-forge gained the strictness spectrum (block / warn / log), false-positive fatigue, and "the block message teaches". rule-forge gained "write judgment, not directive — respect the framework" and explain-the-why. claude-md-forge judged the strongest on content-craft; left essentially as-is.

## 2026-05-29 — Opus 4.8 release re-tests

- **speed-daemon — KEPT.** Two of three skill-withheld mutation trials (inline-edit, delete) reproduced the spinner-gated default; only the toggle came back optimistic; the auth leg reproduced the gated render. The toggle is the easy case — small-n re-tests on it alone would mis-delete.
- **ingest — KEPT.** A skill-withheld agent reproduced the architecture (one reader per source, verbatim quotes, conflict synthesis) only under heavy prompting; the scaffold still buys reliability and the quote contract.
- **claude-md-forge `references/anti-patterns.md` — KEPT.** Blind verbatim cross-project import softened (a skill-withheld trial substituted stack lines and dropped non-transferable ones) but ALWAYS/NEVER and current-state lines persisted, so the file still earns rent. n=1.

## 2026-05-22 — Opus 4.7

- **speed-daemon (serve) — new.** Article-derived (a published Linear performance breakdown). Proxy failure: the React training-data default of spinner-gated mutations and fetch-blocked first paints. Sunset: on each major model release, rebuild several mutation shapes plus an auth-gated page in fresh sessions; delete when optimistic-by-default arrives unaided.
- **ingest (cook) — new.** One reader subagent per source; a brief of verbatim quoted excerpts with citations; handoff to the right meta-forge. Architectural — re-evaluate when grounding-in-sources stops needing explicit scaffolding.

## 2026-05-14 — Opus 4.7

- **skill-forge, hook-forge, rule-forge, claude-md-forge (cook) — authored.** The four meta-forges.
